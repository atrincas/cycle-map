import { Location } from '@/types'

interface Props {
  name: string
  company: string[]
  href: string
  location: Location
}

export function NetworksListItem({ name, company, href, location }: Props) {
  return (
    <section>
      <h2></h2>
    </section>
  )
}
